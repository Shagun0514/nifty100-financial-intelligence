# venv
venv/
.venv/

# python cache
__pycache__/
*.pyc
.pytest_cache/

# secrets
.env

# database (rebuild via `make load`, don't commit binary db)
nifty100.db

# source data (often large/licensed — don't commit raw Excel files)
data/raw/*.xlsx
data/supporting/*.xlsx

# outputs (regenerated by pipeline)
output/*.csv

# OS junk
.DS_Store
Thumbs.db
